{
  "name": "@cross-ui/core-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start:playground": "yarn --cwd playground start",
    "start:core": "yarn --cwd packages/core dev",
    "build": "yarn --cwd packages/core build",
    "build:playground": "yarn --cwd playground build:prod",
    "serve:playground": "yarn --cwd playground serve"
  }
}
